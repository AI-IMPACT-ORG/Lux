module lib.Observers where
open import lib.generated-Core

-- CLEAN v10 Observers - Expanded with Logical Functions

project_L : Term → Term
project_L = λ t → t

project_R : Term → Term
project_R = λ t → t

reconstitute : Term → Term
reconstitute = λ t → t

residual : Term → Term
residual = λ t → t

triality_sum : Term → Term → Term → Term
triality_sum = λ l b r → l
