namespace Observers


-- CLEAN v10 Observers - Expanded with Logical Functions

def project_L : Term → Term :=
  λ t → t

def project_R : Term → Term :=
  λ t → t

def reconstitute : Term → Term :=
  λ t → t

def residual : Term → Term :=
  λ t → t

def triality_sum : Term → Term → Term → Term :=
  λ l b r → l
